# iRacing OAuth Credentials

# ============================================================================
# AUTHORIZATION CODE FLOW (CLI with Browser) - DEFAULT
# ============================================================================
# No configuration needed! Works out of the box with these defaults:
# - IRACING_CLIENT_ID=session-auditor
# - IRACING_REDIRECT_URI=http://127.0.0.1:0/callback
# - Client secret is optional
#
# Just run: iracing-audit --league-id 12345
# ============================================================================

# ============================================================================
# PASSWORD LIMITED FLOW (Headless/Docker) - REQUIRES REGISTRATION
# ============================================================================
# You must register with iRacing to use Password Limited Flow.
# Contact iRacing to obtain a custom client_id and client_secret.
# Up to 3 email addresses can be pre-registered per client.
# ============================================================================

# OAuth Client ID
# Default: 'session-auditor' (works for Authorization Code Flow)
# For Password Limited Flow: Use your custom client_id from iRacing
# IRACING_CLIENT_ID=session-auditor

# OAuth Client Secret
# Optional for Authorization Code Flow
# Required for Password Limited Flow (obtain from iRacing)
# IRACING_CLIENT_SECRET=your_client_secret_here

# Authentication Flow Selection
# Set to "true" for headless/Docker environments (Password Limited Flow)
# Set to "false" or omit for interactive CLI with browser (Authorization Code Flow)
# IRACING_USE_PASSWORD_FLOW=false

# Password Limited Flow Credentials (Required only if IRACING_USE_PASSWORD_FLOW=true)
# Email and password must be pre-registered with iRacing for Password Limited Flow
# IRACING_USERNAME=your_email@example.com
# IRACING_PASSWORD=your_password_here

# OAuth Redirect URI (Optional - only for Authorization Code Flow)
# Default: http://127.0.0.1:0/callback (port 0 = random available port)
# IRACING_REDIRECT_URI=http://127.0.0.1:0/callback

# League Configuration
LEAGUE_ID=your_league_id_here

# Optional: Webhook URL for notifications (e.g., Discord)
# WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_url

# Optional: Path to expectations file
# EXPECTATIONS_PATH=expectations.json

# Optional: Path to state file
# STATE_PATH=state.json

# Keep-alive mode (for continuous monitoring)
# KEEP_ALIVE=false

# Interval between checks in seconds (when keep-alive is enabled)
# INTERVAL=3600

# Force re-validation of all sessions
# FORCE=false
